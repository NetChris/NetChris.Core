name: Release - GitHub

on:
  release:
    types: [released]

jobs:
  push:
    permissions:
      checks: write
      contents: read
      packages: write
    uses: NetChris/workflows/.github/workflows/release-nuget-github.yml@v2
    secrets: inherit
